source "https://rubygems.org"

gem "jekyll", "~> 4.3.0"  # 4.3.x uses jekyll-sass-converter 2.x (sassc), not sass-embedded

# Ruby 4.0 removed these from stdlib — add them back explicitly
gem "csv"
gem "base64"
gem "bigdecimal"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
